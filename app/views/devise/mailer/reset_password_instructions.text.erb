<% instructions = PasswordResetInstructions.new(@token) %>
<%# Canonical fields: reset_password_token, password, password_confirmation. %>
Password reset requested

This service is API-only and does not provide a browser password-reset form.
Use this reset token with PUT /users/password.

Reset token:
<%= instructions.token %>

Content-Type: application/json
JSON payload:
<%= instructions.payload_json %>

curl example:
<%= instructions.curl_command %>

If you did not request this password reset, ignore this email.
