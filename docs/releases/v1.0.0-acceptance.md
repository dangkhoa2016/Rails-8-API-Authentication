# v1.0.0 Acceptance Evidence

## Candidate
- Branch: `release/v1.0.0`
- Accepted implementation SHA: `54efe374325bfa9db1aa58b6d81c7fbad5f4591f`
- Verification date (UTC): `2026-09-08T14:03:35Z`

## Gate status
| Gate | State | Evidence |
| --- | --- | --- |
| Gate 1 — Repository hardening | PASS | GitHub Actions CI run #76 (`34234762773`) completed successfully on exact implementation SHA `54efe374325bfa9db1aa58b6d81c7fbad5f4591f`; security, lint/repository policy, Ruby 3.2/3.3/4.0, custom JWT header, and schema-load jobs all passed. |
| Gate 2 — Release contract | PASS | `release_contract` in CI run #76 passed release tooling tests and repository release-contract verification. |
| Gate 3 — Published artifact verification | PASS | `release_contract` in CI run #76 passed live verification of the frozen PostgreSQL and SQLite GHCR baseline artifacts. |
| Gate 4 — Deployment portability | PASS | The sanitized Beam.cloud release path remained valid and a real Beam deployment completed with the documented `X-Authorization` JWT transport path. |
| Gate 5 — v1.0.0 readiness evidence | PASS | Exact-candidate CI PASS + live frozen-artifact verification + repository controls active + real Beam full smoke PASS for `/up`, `/users/sign_in`, and `/user/profile`; no release blocker is observed in the evidence below. |

## CI evidence
- Workflow: `CI`
- Run: `#76`
- Run ID: `34234762773`
- Candidate SHA: `54efe374325bfa9db1aa58b6d81c7fbad5f4591f`
- Result: `completed / success`
- Required jobs observed PASS:
  - `audit_gems`
  - `scan_ruby`
  - `release_contract`
  - `lint`
  - `schema_load`
  - `test_custom_header`
  - `test (3.2)`
  - `test (3.3)`
  - `test (4.0)`

## Frozen baseline invariants
- PostgreSQL source: `6897c773ec1321401e52c21c63870a72d01ca349`
- SQLite source: `1d842b18c1d1b07c027cbb7d49c19a52d16f98bc`
- Immutable tags remain `baseline-postgresql-v1` and `baseline-sqlite-v1`.

## Deployment acceptance
- Provider: Beam.cloud
- Deployment URL: `https://778daf93-9b0b-4157-a4f7-4feca261e661-8080.app.beam.cloud`
- JWT transport header: `X-Authorization`
- `/up`: PASS — HTTP 200
- `/users/sign_in`: PASS — JWT received through `X-Authorization` or JSON fallback
- `/user/profile`: PASS — HTTP 200
- Deployment smoke summary: PASS — health + authentication

Observed sanitized smoke output:

```text
[release] PASS    deployment health            https://778daf93-9b0b-4157-a4f7-4feca261e661-8080.app.beam.cloud/up -> 200
[release] PASS    authentication sign-in       JWT received via X-Authorization or JSON fallback
[release] PASS    authenticated profile        /user/profile -> 200
[release] PASS    deployment smoke             health + authentication
```

## Runtime provenance

The accepted stable implementation/source candidate is:

`54efe374325bfa9db1aa58b6d81c7fbad5f4591f`

The Beam recipe intentionally derives from the frozen PostgreSQL runtime image:

`ghcr.io/dangkhoa2016/rails-8-api-authentication:postgresql-6897c77`

whose frozen source baseline is:

`6897c773ec1321401e52c21c63870a72d01ca349`

Accordingly, the deployment smoke proves a real Beam deployment and the documented JWT transport/authentication path against the frozen PostgreSQL application runtime. It does not claim that the running application container itself was built from stable candidate SHA `54efe374325bfa9db1aa58b6d81c7fbad5f4591f`.

## Repository controls
- Ruleset ID: `22532104`
- Name: `require before merge or push`
- Enforcement: `active`
- Protected refs include `main`, `baseline/postgresql-v1`, and `baseline/sqlite-v1`.
- Required checks: all nine release-required contexts.
- Bypass actors: none.
- `current_user_can_bypass`: `never`.

## Evidence lineage

This is an evidence-only commit. The accepted implementation SHA remains `54efe374325bfa9db1aa58b6d81c7fbad5f4591f`. The commit that adds this file records acceptance evidence and must not replace that implementation SHA as the provenance reference for `v1.0.0`.

The evidence-only branch head must itself pass repository policy and GitHub CI before PR #5 is considered merge-ready.

## Release blockers
- None observed at evidence-record creation time.
