# v1.0.0 Acceptance Evidence

## Candidate
- Branch: `release/v1.0.0-ci-push-policy`
- Accepted implementation SHA: `b73d8e1acbb1017fb4b384f254c1347645e801ba`
- Verification date (UTC): `2026-09-08T14:29:39Z`

## Gate status
| Gate | State | Evidence |
| --- | --- | --- |
| Gate 1 — Repository hardening | PASS | GitHub Actions CI run #81 (`34238481268`) completed successfully on exact corrective implementation SHA `b73d8e1acbb1017fb4b384f254c1347645e801ba`; security, lint/repository policy, Ruby 3.2/3.3/4.0, custom JWT header, and schema-load jobs all passed. |
| Gate 2 — Release contract | PASS | `release_contract` in CI run #81 passed release tooling tests and repository release-contract verification. |
| Gate 3 — Published artifact verification | PASS | `release_contract` in CI run #81 passed live verification of the frozen PostgreSQL and SQLite GHCR baseline artifacts. |
| Gate 4 — Deployment portability | PASS | The corrective source delta is limited to `.github/workflows/ci.yml` and `scripts/release/test_release_tools.sh`; no application, authentication, Beam recipe, or runtime image content changed. The previously observed full Beam smoke therefore remains applicable to the unchanged deployment path. |
| Gate 5 — v1.0.0 readiness evidence | PASS | Exact-candidate CI PASS + live frozen-artifact verification + repository controls active + real Beam full smoke PASS for `/up`, `/users/sign_in`, and `/user/profile`; no release blocker is observed in the evidence below. |

## CI evidence
- Workflow: `CI`
- Run: `#81`
- Run ID: `34238481268`
- Candidate SHA: `b73d8e1acbb1017fb4b384f254c1347645e801ba`
- Result: `completed / success`
- Required jobs observed PASS:
  - `audit_gems`
  - `scan_ruby`
  - `release_contract`
  - `lint`
  - `schema_load`
  - `test_custom_header`
  - `test (3.2)`
  - `test (3.3)`
  - `test (4.0)`

## Post-merge corrective context

The original release PR #5 merged to `main` at `047ddf95cc80697d78deb6b4aa5df736c7b84ba2`. Push CI #78 then exposed a release-engineering wiring issue: push-to-main repository-policy verification had no incremental base and audited historical pre-release commits.

Corrective PR #6 changes only:

- `.github/workflows/ci.yml`
- `scripts/release/test_release_tools.sh`

The corrective workflow resolves the repository-policy base by event:

- pull request: exact pull-request base SHA;
- push: `github.event.before` (the pre-push SHA);
- manual dispatch: `HEAD^` fallback.

A regression test was first introduced and observed failing in CI #79 before the workflow correction. CI #81 then passed the complete required matrix on the final corrective implementation candidate. The schema-load safety contract (`ALLOW_DATABASE_RESET=1`, isolated schema-test database, and `PGPASSWORD`) was preserved/restored before qualification.

## Frozen baseline invariants
- PostgreSQL source: `6897c773ec1321401e52c21c63870a72d01ca349`
- SQLite source: `1d842b18c1d1b07c027cbb7d49c19a52d16f98bc`
- Immutable tags remain `baseline-postgresql-v1` and `baseline-sqlite-v1`.

## Deployment acceptance
- Provider: Beam.cloud
- Deployment URL: `https://778daf93-9b0b-4157-a4f7-4feca261e661-8080.app.beam.cloud`
- JWT transport header: `X-Authorization`
- `/up`: PASS — HTTP 200
- `/users/sign_in`: PASS — JWT received through `X-Authorization` or JSON fallback
- `/user/profile`: PASS — HTTP 200
- Deployment smoke summary: PASS — health + authentication

Observed sanitized smoke output:

```text
[release] PASS    deployment health            https://778daf93-9b0b-4157-a4f7-4feca261e661-8080.app.beam.cloud/up -> 200
[release] PASS    authentication sign-in       JWT received via X-Authorization or JSON fallback
[release] PASS    authenticated profile        /user/profile -> 200
[release] PASS    deployment smoke             health + authentication
```

This smoke was observed before the CI-only corrective. Because PR #6 changes no application/authentication/deployment files, it remains evidence for the unchanged Beam runtime path; it is not treated as proof that the Beam application container was rebuilt from the corrective candidate SHA.

## Runtime provenance

The accepted stable implementation/source candidate is:

`b73d8e1acbb1017fb4b384f254c1347645e801ba`

The Beam recipe intentionally derives from the frozen PostgreSQL runtime image:

`ghcr.io/dangkhoa2016/rails-8-api-authentication:postgresql-6897c77`

whose frozen source baseline is:

`6897c773ec1321401e52c21c63870a72d01ca349`

Accordingly, the deployment smoke proves a real Beam deployment and the documented JWT transport/authentication path against the frozen PostgreSQL application runtime. It does not claim that the running application container itself was built from stable candidate SHA `b73d8e1acbb1017fb4b384f254c1347645e801ba`.

## Repository controls
- Ruleset ID: `22532104`
- Name: `require before merge or push`
- Enforcement: `active`
- Protected refs include `main`, `baseline/postgresql-v1`, and `baseline/sqlite-v1`.
- Required checks: all nine release-required contexts.
- Bypass actors: none.
- `current_user_can_bypass`: `never`.

## Evidence lineage

This acceptance update is evidence-only. The accepted implementation SHA is `b73d8e1acbb1017fb4b384f254c1347645e801ba`. The commit that updates this file records acceptance evidence and must not replace that implementation SHA as the provenance reference for `v1.0.0`.

The evidence-only branch head must itself pass repository policy and GitHub CI before corrective PR #6 is considered merge-ready.

## Release blockers
- None observed at corrective evidence-record creation time.
