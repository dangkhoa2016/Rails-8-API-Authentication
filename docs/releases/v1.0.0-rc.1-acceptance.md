# v1.0.0-rc.1 Acceptance Evidence

## Candidate
- Branch: `release/v1.0.0-rc.1`
- Candidate SHA: `98c9a370ae96a6112531d5e3f754d2723fb0e470`
- Verification date (UTC): `2026-09-08T13:17:57Z`

## Gate status
| Gate | State | Evidence |
| --- | --- | --- |
| Gate 1 — Repository hardening | PASS | GitHub Actions CI run #74 (`34228105671`) completed successfully on candidate SHA `98c9a370ae96a6112531d5e3f754d2723fb0e470`; required security, lint, repository-policy, Ruby 3.2/3.3/4.0, custom-header, schema-load, and release-contract jobs passed. |
| Gate 2 — Release contract | PASS | `release_contract` job in CI run #74: release tooling tests and `scripts/release/verify_repository.sh` completed successfully. |
| Gate 3 — GHCR verification | PASS | `release_contract` job in CI run #74: `Verify published GHCR baseline images` completed successfully for the frozen PostgreSQL and SQLite baseline contracts. |
| Gate 4 — Deployment portability | PASS | Repository/static deployment contract passed in CI run #74; Beam deployment executed successfully using the sanitized Beam recipe and runtime secrets. |
| Gate 5 — RC readiness | PASS | Exact-candidate CI PASS plus real Beam.cloud full smoke PASS for `/up`, `/users/sign_in`, and `/user/profile`; GitHub ruleset `22532104` is active with no bypass actors. |

## Baseline invariants
- PostgreSQL: `6897c773ec1321401e52c21c63870a72d01ca349`
- SQLite: `1d842b18c1d1b07c027cbb7d49c19a52d16f98bc`

## Deployment acceptance
- Provider: Beam.cloud
- Deployment URL: `https://a62a4bfd-fcff-4b4b-b13f-bc9385784d83-8080.app.beam.cloud`
- JWT transport header: `X-Authorization`
- `/up`: PASS — HTTP 200
- `/users/sign_in`: PASS — JWT received via `X-Authorization` or JSON fallback
- `/user/profile`: PASS — HTTP 200
- Deployment smoke summary: PASS — health + authentication

Observed sanitized smoke output:

```text
[release] PASS    deployment health            https://a62a4bfd-fcff-4b4b-b13f-bc9385784d83-8080.app.beam.cloud/up -> 200
[release] PASS    authentication sign-in       JWT received via X-Authorization or JSON fallback
[release] PASS    authenticated profile        /user/profile -> 200
[release] PASS    deployment smoke             health + authentication
```

## Runtime provenance

The accepted implementation/source candidate is `98c9a370ae96a6112531d5e3f754d2723fb0e470`.

The Beam recipe intentionally derives from the frozen PostgreSQL runtime image:

`ghcr.io/dangkhoa2016/rails-8-api-authentication:postgresql-6897c77`

whose frozen source baseline is:

`6897c773ec1321401e52c21c63870a72d01ca349`

Accordingly, this deployment smoke proves the accepted candidate's Beam deployment tooling and custom JWT transport path against the frozen PostgreSQL application runtime. It does not claim that the running application container itself was built from candidate SHA `98c9a370ae96a6112531d5e3f754d2723fb0e470`.

This provenance model matches the repository release contract, which verifies the exact candidate through CI/repository checks while separately verifying frozen GHCR baseline artifacts and requiring at least one real full deployment smoke.

## Repository controls
- Ruleset ID: `22532104`
- Name: `require before merge or push`
- Enforcement: active
- Bypass actors: none
- `current_user_can_bypass`: `never`

## Evidence lineage

This file is an evidence-only record. The accepted implementation SHA remains `98c9a370ae96a6112531d5e3f754d2723fb0e470`. The commit that adds this evidence file is not intended to replace that accepted implementation SHA for the `v1.0.0-rc.1` tag.

## Release blockers
- None observed at acceptance time.
